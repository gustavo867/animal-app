import { StyleSheet, Dimensions } from 'react-native';

const styles = StyleSheet.create({
  image: {
    width: 45,
    height: 45,
    borderRadius: 22.5,
    marginRight: 20,
    borderColor: '#FFB627',
    borderWidth: 1.5,
  },
})

export default styles